基于你刚才的环境失败结果，以及当前 `feature/complete-reference-style-pgt` 分支代码，我建议不要继续围绕 NATTEN 做版本兼容，而是把 **OneFormer 的实现路径改成不依赖 DiNAT/NATTEN 的 HuggingFace Swin backend**。

还有一个更重要的问题：当前远端 `semantic_regions.py` 里的生产语义路径实际上还是旧结构——`GroundingDINO + SAM2.1 + SCHP + OneFormer sky-only`，并没有完整落实 0810SEGSPEC 里冻结的：

```text
OneFormer
= 主 Panoptic segmentation
= person instances + sky + vegetation

SAM2.1
= 低质量 person refinement

Face/Skin parser
= face / skin
```

当前代码中 `OneFormerADE20KSkyParser` 甚至只抽取 `sky`，而 person 仍来自 Grounding DINO。 这与 0810SEGSPEC 的最终设计并不一致。

因此这次不应该只做“把 DiNAT checkpoint 换成 Swin checkpoint”这么小的 patch，而应该顺势把 segmentation backend **真正收敛到 0810SEGSPEC**。

---

# 一、最终修改目标

修改前：

```text
Grounding DINO
   ↓
person boxes
   ↓
SAM2.1
   ↓
human masks

SCHP
   ↓
face / skin

OneFormer DiNAT
   ↓
sky only
```

修改后：

```text
                     Image
                       │
             ┌─────────┴─────────┐
             ▼                   ▼
       OneFormer-Swin       Face/Skin Parser
         Panoptic                │
             │                   ├─ face
      ┌──────┼───────┐           └─ skin
      ▼      ▼       ▼
   person   sky   vegetation
     │
     ▼
Person Quality Check
     │
  good ──────────────┐
                     │
  bad                │
   ↓                 │
 SAM2.1              │
 refinement          │
   └─────────┬───────┘
             ▼
       Person Instances
             │
             ▼
 Face/Skin ↔ Person association
             │
             ▼
 aggregate masks
 +
 exclusive analysis partitions
 +
 interior ROI
             │
             ▼
 InstanceAwareSemanticBundle
```

这一结构和 0810SEGSPEC 完全一致：OneFormer 是主场景/实例理解，SAM2.1 只修坏 person mask。

---

# 二、Task 1：替换 OneFormer backend

## 当前问题

当前：

```python
class OneFormerADE20KSkyParser(_LazyLocalBackend):
```

职责只有：

```python
sky_probability()
```

而且注释直接写：

```text
OneFormer ADE20K DiNAT-L
```

建议删除这个类，改成：

```python
class OneFormerPanopticBackend(_LazyLocalBackend):
```

接口：

```python
@dataclass
class OneFormerPanopticOutput:
    persons: list[RawPersonInstance]
    sky: np.ndarray
    vegetation: np.ndarray
    fine_grained_masks: dict[str, np.ndarray]
```

主接口：

```python
class OneFormerPanopticBackend:

    def analyze(
        self,
        image: np.ndarray,
    ) -> OneFormerPanopticOutput:
        ...
```

---

# 三、Task 2：默认 checkpoint 改成 Swin

不要继续使用：

```text
OneFormer + DiNAT
       ↓
NATTEN
```

默认改成：

```text
OneFormer + Swin
```

建议配置：

```yaml
semantic_segmentation:

  oneformer:
    enabled: true
    backend: huggingface
    model: shi-labs/oneformer_ade20k_swin_tiny
    task: panoptic

    device: cuda
    dtype: bfloat16
```

第一版优先：

```text
Swin-Tiny
```

原因不是它理论性能最高，而是目前首先需要验证：

```text
person instance
sky
vegetation
```

对 Tone Statistics 是否稳定。

如果真实数据验证发现不足：

```text
Swin-Tiny
   ↓
Swin-Large
```

只换 checkpoint，不改 architecture。

---

# 四、Task 3：彻底移除 NATTEN mandatory dependency

需要检查所有：

```text
requirements
environment
Docker
README
model setup docs
runtime check
```

删除：

```text
natten
dinat
```

作为强制依赖。

生产路径不再出现：

```python
import natten
```

也不应该出现：

```text
NATTEN wheel installation
CUDA architecture compatibility
```

这样的 setup prerequisite。

新的运行依赖收敛成：

```text
torch
transformers
Pillow
numpy
SAM2.1 dependencies
face/skin parser dependencies
```

核心目标就是：

> Blackwell 环境不再因为 OneFormer segmentation 编译 CUDA extension。

---

# 五、Task 4：OneFormer 改成真正 Panoptic inference

当前代码：

```python
task_inputs=["semantic"]
```

然后：

```python
post_process_semantic_segmentation(...)
```

需要改成：

```python
task_inputs=["panoptic"]
```

逻辑：

```python
outputs = model(**inputs)

panoptic = processor.post_process_panoptic_segmentation(
    outputs,
    target_sizes=[image.shape[:2]],
)[0]
```

然后获得：

```text
segmentation
segments_info
```

遍历：

```python
for segment in segments_info:
    segment_id = segment["id"]
    label_id = segment["label_id"]

    label = id2label[label_id]
```

---

# 六、Task 5：从 OneFormer 直接构建 person instances

删除当前 person 主路径：

```text
Grounding DINO
→ person box
→ SAM2
```

换成：

```text
OneFormer panoptic
→ person segments
→ person_i
```

建议数据结构：

```python
@dataclass
class PersonInstance:
    instance_id: int

    mask: np.ndarray
    interior_mask: np.ndarray

    bbox: tuple[int, int, int, int]
    area_ratio: float
    confidence: float

    source_segment_id: int

    face_mask: np.ndarray | None = None
    skin_mask: np.ndarray | None = None

    refined_by_sam: bool = False
```

和 SPEC 定义保持一致。

---

# 七、Task 6：Grounding DINO 从生产 segmentation path 删除

这次建议不是保留成 fallback，而是直接移出 PGT segmentation 主流程。

删除：

```python
HumanDetector
GroundingDINOHumanDetector
text_prompt
box_threshold
text_threshold
nms_iou_threshold
```

因为现在：

```text
OneFormer person instance
```

已经负责人物发现。

否则会重新出现 0810SEGSPEC 明确禁止的职责重复：

```text
OneFormer person
+
Grounding DINO person
+
SAM person
```



---

# 八、Task 7：SAM2.1 从“默认 segmenter”改成 conditional refiner

这是非常重要的一步。

当前：

```text
Grounding DINO box
→ 每个人都跑 SAM
```

要改成：

```text
OneFormer person
→ quality check
→ only bad masks → SAM2.1
```

新增：

```python
def needs_sam_refine(
    person: PersonInstance,
    config: PersonRefineConfig,
) -> bool:
```

第一版只需要几个简单规则：

```text
confidence
connected component count
bbox occupancy
area ratio
compactness
```

例如：

```python
if person.confidence < min_confidence:
    return True

if connected_component_count(person.mask) > max_components:
    return True

if bbox_occupancy(person.mask, person.bbox) < min_bbox_occupancy:
    return True
```

不要再继续增加复杂 quality model。

---

# 九、Task 8：SAM2.1 输入改成 OneFormer coarse instance

推荐：

```text
Image
+
OneFormer bbox
+
optional coarse person mask
```

输出：

```text
refined_mask
```

然后只做必要 sanity check：

```text
与原 bbox overlap 合理
面积没有异常变化
没有明显吞并其他 person
```

如果失败：

```python
person.mask = original_oneformer_mask
person.refined_by_sam = False
```

这也是 SPEC 中冻结的 fallback。

---

# 十、Task 9：SAM2.1 模型改 Base+

当前代码类名：

```python
SAM21HieraLargeSegmenter
```

建议改成通用：

```python
SAM21PersonRefiner
```

默认配置：

```yaml
sam21:
  enabled: true
  refine_only: true
  model_size: base_plus
```

不要把 Large 固定进 class 名。

因为 SAM 只是 fallback，不是主 segmentation backbone。

---

# 十一、Task 10：OneFormer 同时输出 sky + vegetation

当前 OneFormer 只提取：

```text
sky
```

需要增加：

```python
VEGETATION_LABELS = {
    "tree",
    "grass",
    "plant",
    "flower",
    "palm",
}
```

运行：

```python
vegetation_mask = np.zeros((h, w), dtype=np.float32)

for segment in segments:
    if label_name in VEGETATION_LABELS:
        vegetation_mask = np.maximum(
            vegetation_mask,
            segment_mask,
        )
```

同时保留：

```python
fine_grained_masks = {
    "tree": ...,
    "grass": ...,
    "plant": ...,
    ...
}
```

但 Renderer / statistics 默认只消费：

```text
vegetation
```

这与 SPEC 一致。

---

# 十二、Task 11：Face/Skin parser 保留独立职责

这里不要让 OneFormer 顺便接管 face / skin。

继续：

```text
Lightweight Face/Skin Parser
```

输出：

```text
face
skin
face_skin
body_skin
```

然后：

```python
person.face_mask = global_face & person.mask
person.skin_mask = global_skin & person.mask
```

多人时 face component 优先采用：

```text
face center inside person
+
face-person overlap
```

进行 assignment。

---

# 十三、Task 12：建立真正的 InstanceAwareSemanticBundle

建议不要继续只输出目前：

```python
SemanticRegionSet
```

而新增：

```python
@dataclass
class InstanceAwareSemanticBundle:
    image_height: int
    image_width: int

    persons: list[PersonInstance]

    human: np.ndarray
    face: np.ndarray
    skin: np.ndarray

    sky: np.ndarray
    vegetation: np.ndarray
    background: np.ndarray

    analysis_face: np.ndarray
    analysis_body_skin: np.ndarray
    analysis_human_non_skin: np.ndarray
    analysis_sky: np.ndarray
    analysis_vegetation: np.ndarray
    analysis_background: np.ndarray

    quality: SemanticQualityReport
```



但是为了避免一次修改整个 V4 pipeline，可以保留：

```python
bundle.to_legacy_region_set()
```

提供：

```text
human
face
skin
sky
background
```

给现有代码。

所以这里推荐：

```text
新接口成为 truth
↓
legacy adapter 保兼容
```

而不是反过来。

---

# 十四、Task 13：构建 aggregate human

直接：

```python
human = union(
    person.mask
    for person in persons
)
```

而不是再跑一个独立 human segmentation model。

这解决两个问题：

```text
instance masks 与 aggregate human 天然一致
person count 与 human area 天然一致
```

---

# 十五、Task 14：增加互斥 analysis partitions

这是 PGT 真正使用的 mask。

构建：

```python
analysis_face = face

analysis_body_skin = (
    skin
    * (1 - face)
)

analysis_human_non_skin = (
    human
    * (1 - skin)
)

analysis_vegetation = (
    vegetation
    * (1 - human)
)

analysis_sky = (
    sky
    * (1 - human)
    * (1 - vegetation)
)

analysis_background = (
    1
    - human
    - sky
    - vegetation
)
```

最终：

```text
face
body_skin
human_non_skin
vegetation
sky
background
```

尽量互斥。

这一部分直接服务：

```text
Region Tone
Region Color
Relationship loss
```

而不是 segmentation benchmark。

---

# 十六、Task 15：Interior ROI

新增统一函数：

```python
def build_interior_mask(
    mask: np.ndarray,
    erode_ratio: float,
) -> np.ndarray:
```

不要使用固定像素：

```text
erode 5px
```

而使用：

```text
relative to image size / region size
```

最终：

```text
full mask
→ visualization / association

interior mask
→ statistics
```

例如：

```text
face median
skin chroma
human EV
sky highlight
vegetation tone
```

全部默认使用 interior。

---

# 十七、Task 16：SemanticQualityReport

新增：

```python
@dataclass
class SemanticQualityReport:
    person_count: int
    person_confidence: tuple[float, ...]

    face_count: int
    face_person_assignment_confidence: float

    human_area_ratio: float
    face_area_ratio: float
    skin_area_ratio: float
    sky_area_ratio: float
    vegetation_area_ratio: float

    sam_refined_person_count: int

    warnings: tuple[str, ...]
```

但是不要把 rubric 做得太复杂。

后续只需要能够判断：

```text
use
lower weight
skip
```

即可。

---

# 十八、Task 17：配置重写

当前 `SegmentationConfig` 里包含：

```text
text_prompt
box_threshold
text_threshold
NMS
```

这些都是 Grounding DINO 遗留。

建议改成：

```python
@dataclass
class OneFormerConfig:
    model_path: str
    task: str = "panoptic"
    device: str = "cuda"
    dtype: str = "bfloat16"

    min_person_area_ratio: float = 5e-4
    min_region_area_ratio: float = 1e-5
```

```python
@dataclass
class PersonRefineConfig:
    enabled: bool = True
    min_confidence: float = 0.5
    max_component_count: int = 3
    min_bbox_occupancy: float = 0.15
```

```python
@dataclass
class SemanticSegmentationConfig:
    oneformer: OneFormerConfig
    person_refine: PersonRefineConfig
    ...
```

---

# 十九、Task 18：不要在代码里写死 DiNAT

必须全文搜索：

```text
dinat
natten
DiNAT-L
```

生产代码、README、sample config 中全部去掉。

OneFormer backend 只应该知道：

```text
checkpoint path
```

例如：

```python
OneFormerProcessor.from_pretrained(model_path)
OneFormerForUniversalSegmentation.from_pretrained(model_path)
```

它不应该知道 backbone 是：

```text
Swin
ConvNeXt
DiNAT
```

这样未来换 Swin-L 完全不需要改代码。

---

# 二十、Task 19：错误信息必须保留真实原因

当前 `_LazyLocalBackend`：

```python
except Exception:
    raise RuntimeError(
        f"local backend unavailable: ..."
    ) from None
```

会把真正的问题隐藏掉。

这对你这次 NATTEN 问题定位非常不利。

改成：

```python
except Exception as exc:
    raise RuntimeError(
        f"OneFormer backend failed: {exc}"
    ) from exc
```

这是正常工程可诊断性，不需要设计复杂 exception framework。

至少应该让：

```text
CUDA architecture sm_120 unsupported
```

这样的真实错误能够看到。

---

# 二十一、Task 20：Source / Reference 必须复用同一 producer

V4 pipeline：

```python
semantic_producer.analyze(source)
semantic_producer.analyze(reference)
```

两个调用必须完全相同。

不能：

```text
Source → Swin
Reference → DiNAT
```

也不能：

```text
Source → OneFormer
Reference → fallback SegFormer
```

否则 region statistics 会有系统偏差。

这是 0810SEGSPEC 的明确 contract。

---

# 二十二、测试修改

## Test A — OneFormer backend 不依赖 NATTEN

测试重点不是：

```python
assert "natten" not in pip
```

而是代码级：

```text
mock OneFormer Swin
→ initialize
→ panoptic inference
→ 不 import natten
```

---

## Test B — Panoptic person instance

合成：

```text
person_0
person_1
sky
tree
```

mock OneFormer output。

要求：

```python
len(bundle.persons) == 2
```

并且：

```text
human = person0 ∪ person1
```

---

## Test C — Vegetation aggregation

输入：

```text
tree
grass
plant
```

要求：

```text
vegetation =
tree ∪ grass ∪ plant
```

---

## Test D — SAM only bad instance

构造：

```text
person_0 confidence=0.95
person_1 confidence=0.30
```

要求：

```text
SAM calls == 1
```

而不是 2。

---

## Test E — SAM failure fallback

SAM 抛异常：

```text
person.mask == original OneFormer mask
```

pipeline 继续。

---

## Test F — Face/person association

两个人 + 两张脸。

确认：

```text
face0 → person0
face1 → person1
```

---

## Test G — Analysis partitions mutually exclusive

确认：

```text
face ∩ body_skin == 0

body_skin ∩ human_non_skin == 0

human ∩ vegetation == 0
```

在 threshold 后成立。

---

## Test H — Empty person is valid

没有人：

```text
persons = []
human = empty
face = empty
skin = empty
```

不报错。

---

# 二十三、真实 smoke test

这比 unit test 更重要。

至少准备：

```text
10 单人大人像
10 多人
10 小人物/远景人物
10 天空
10 vegetation
```

重点统计：

```text
person detection success

person merge error

person fragmentation

SAM refinement ratio

face-person assignment error

sky obvious false positive

vegetation obvious false positive
```

但最终项目级指标应该是：

```text
mask perturbation
      ↓
face median luminance 变化多少
human EV 变化多少
sky highlight P95 变化多少
vegetation median 变化多少
```

因为 segmentation 模块的目的本来就不是追求 segmentation leaderboard，而是给 Tone/Color Statistics 提供稳定 ROI。

---

# 二十四、建议文件修改清单

| 文件                                 | 修改                                                                        |
| ---------------------------------- | ------------------------------------------------------------------------- |
| `semantic_regions.py`              | **核心重构**：OneFormer Panoptic + person/sky/vegetation；删除 Grounding DINO 主路径 |
| `__init__.py`                      | export 新 `InstanceAwareSemanticBundle` / `PersonInstance`                 |
| `v4_pipeline.py`                   | Source/Reference 统一调用新 producer                                           |
| `v4_style.py`                      | statistics 优先消费 analysis partitions / interior ROI                        |
| `v4_visualization.py`              | 增加 person instances、vegetation、partitions、SAM-refined 标记                  |
| runtime config                     | checkpoint 改为 OneFormer Swin                                              |
| dependency 文件                      | 移除 NATTEN / DiNAT mandatory dependency                                    |
| `tests/...semantic...`             | panoptic/person/SAM fallback/vegetation/partitions tests                  |
| `tests/test_reference_style_v4.py` | 确认 V4 target 可消费新 semantic bundle                                         |
| README / setup docs                | 删除 NATTEN Blackwell 安装要求                                                  |

---

# 二十五、Builder 实施顺序

不要一次性把整个 pipeline 改烂。

建议严格：

```text
Task 1
OneFormer Swin Panoptic backend
↓
mock tests

Task 2
person / sky / vegetation extraction
↓
tests

Task 3
InstanceAwareSemanticBundle
↓
tests

Task 4
conditional SAM2.1 refinement
↓
tests

Task 5
face / skin person association
↓
tests

Task 6
aggregate + exclusive partitions + interior ROI
↓
tests

Task 7
legacy SemanticRegionSet adapter
↓
旧 V4 tests green

Task 8
pipeline integration
↓
full tests

Task 9
dependency cleanup
NATTEN / DiNAT removal
↓
Blackwell runtime smoke

Task 10
real-image segmentation smoke
↓
Tone Statistics stability test
```

---

# 二十六、这次明确不要做什么

不要因为 OneFormer 环境问题又扩大模型栈：

```text
不要改回 Grounding DINO 主 person detector
不要加 SegFormer person
不要加 SAM3
不要加 Grounded-SAM
不要加 open-vocabulary detector
不要加 ReID
不要加 matting
不要训练新的 segmentation model
```

同样也不要继续：

```text
试 natten 0.18
试 natten 0.19
试各种 CUDA flags
```

这会继续把时间耗在一个对 PGT 没有核心价值的依赖上。

---

# 最终推荐方案

这次修改应该定义成：

> **不是“让旧 DiNAT OneFormer 支持 Blackwell”，而是把当前 segmentation 实现修正成真正符合 0810SEGSPEC 的 OneFormer-Swin Panoptic architecture。**

最终代码结构：

```text
OneFormer-Swin
    ↓
Panoptic
    ├─ person instances
    ├─ sky
    └─ vegetation
          ↓
bad person only
          ↓
SAM2.1 Base+
          ↓

Face/Skin Parser
          ↓

InstanceAwareSemanticBundle
          ↓
exclusive interior ROI
          ↓
Tone / Color Statistics
```

这样同时解决两个问题：

1. **工程问题**：完全绕开当前 `NATTEN × Blackwell sm_120` blocker；
2. **架构问题**：把当前仍然存在的 `Grounding DINO + SAM2 + SCHP + OneFormer sky-only` 旧生产路径真正修正成 0810SEGSPEC 冻结架构。当前远端分支 HEAD 是 `300b8a66...`。

另外，整个修改不会影响 PGT 的核心原则：semantic segmentation 只是为区域 Tone/Color Statistics 提供 ROI；最终 PGT 仍由 Source-Locked Photometric Renderer 从 Source 生成。
